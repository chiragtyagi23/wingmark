/**
 * NOTE: All website content has moved to plain JSON files under `src/api/*.json`.
 * Components import those JSON modules directly - no fetch layer, no client.
 *
 * This file is intentionally kept thin and only exports the site URL.
 */

export const SITE_URL = 'https://wingmark.vercel.app';
